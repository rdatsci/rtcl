#!/usr/bin/env Rscript

library("methods")
library("rt")
doc = "
Usage:
  rupdate [--neverupgrade --rebuild] <path>
Options:
  --neverupgrade Does not update dependencies.
  --rebuild      Rebuild packages which are built against an older version of R.
"
rt:::cli.call("rupdate", doc, commandArgs(TRUE))

# vim: ft=r
