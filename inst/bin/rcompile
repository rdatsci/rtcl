#!/usr/bin/env Rscript

library("methods")
library("rt")
doc = "
Usage:
  rcompile [--html] <files>...

Options:
  --html  Convert to HTML (default is PDF)
"
rt:::cli.call("rcompile", doc, commandArgs(TRUE))

# vim: ft=r
