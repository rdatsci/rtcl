#!/usr/bin/env Rscript

library("methods")
library("rt")
doc = "
Usage:
  rknit [--html] <file>

Options:
  --html  Convert to HTML (default is PDF)
"
rt:::cli.call("rknit", doc, commandArgs(TRUE))

# vim: ft=r
