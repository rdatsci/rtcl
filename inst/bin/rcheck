#!/usr/bin/env Rscript

library("methods")
library("rtcl")
doc = "
Usage:
  rcheck [--cleanup] [<path>]
Options:
  --cleanup    Cleanup log files in temp directory.
"
rtcl:::cli.call("rcheck", doc, commandArgs(TRUE))

# vim: ft=r
