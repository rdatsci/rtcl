#!/usr/bin/env Rscript

library("methods")
library("rt")
doc = "
Usage:
  rcheck [--nocleanup] [<path>]
Options:
  --nocleanup    Do not cleanup log files in temp directory.
"
rt:::cli.call("rcheck", doc, commandArgs(TRUE))

# vim: ft=r
