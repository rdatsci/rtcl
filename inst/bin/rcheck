#!/usr/bin/env Rscript

library("methods")
library("rt")
doc = "
Usage:
  rcheck [--cleanup] [<path>]
Options:
  --cleanup    Cleanup log files in temp directory.
"
rt:::cli.call("rcheck", doc, commandArgs(TRUE))

# vim: ft=r
