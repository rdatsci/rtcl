#!/usr/bin/env Rscript

library("methods")
library("rt")
doc = "
Usage:
  rhub [--platform=<platform>] [<path>]
Options:
  --platform=<platform>  Name of the platform to check on
"
rt:::cli.call("rhub", doc, commandArgs(TRUE))

# vim: ft=r
