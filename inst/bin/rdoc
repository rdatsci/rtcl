#!/usr/bin/env Rscript

library("methods")
library("rt")
doc = "
Usage:
  rdoc [--staticdocs] [<path>]
Options:
  --staticdocs  Also run 'staticdocs' on the generated man pages
"
rt:::cli.call("rdoc", doc, commandArgs(TRUE))

# vim: ft=r
