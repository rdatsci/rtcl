#!/usr/bin/env Rscript

library("methods")
library("rt")
doc = "
Usage:
  rmarkdown [--html] <file>

Options:
  --html  Convert to HTML (default is PDF)
"
rt:::cli.call("rmarkdown", doc, commandArgs(TRUE))

# vim: ft=r
