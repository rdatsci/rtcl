#!/usr/bin/env Rscript

library("methods")
library("rt")
doc = "
Usage:
  rclone <pkg>

Options:
  --temp  Clone to temp directory instead of current directory?
"
rt:::cli.call("rclone", doc, commandArgs(TRUE))

# vim: ft=r
