#!/usr/bin/env Rscript

library("methods")
library("rtcl")
doc = "
Usage:
  rshine [--port=<number>] [<path>]
Options:
  --port=<number>   Port for shiny::runApp().
"
rtcl:::cli.call("rshine", doc, commandArgs(TRUE))

# vim: ft=r
