#!/usr/bin/env Rscript

library("methods")
library("rt")
doc = "
Usage:
  rshine [--port=<number>] [<path>]

Options:
  --port=<number> Port for shiny::runApp().
"
rt:::cli.call("rshine", doc, commandArgs(TRUE))

# vim: ft=r
