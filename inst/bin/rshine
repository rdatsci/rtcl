#!/usr/bin/env Rscript

library("methods")
library("rt")
doc = "
Usage:
  rshine [--port] [<path>]
Options:
  --port=0  Port for shiny::runApp().
"
rt:::cli.call("rshine", doc, commandArgs(TRUE))

# vim: ft=r
