#!/usr/bin/env Rscript

library("methods")
library("rt")
doc = "
Usage:
  rtest [--filter=<pattern>] [<path>]
Options:
  --filter=<pattern>  Restict tests to tests with filenames matching this pattern
"
rt:::cli.call("rtest", doc, commandArgs(TRUE))

# vim: ft=r
