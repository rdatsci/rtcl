#!/usr/bin/env Rscript

library("methods")
library("rtcl")
doc = "
Usage:
  rtest [--filter=<pattern>] [<path>]
Options:
  --filter=<pattern>  Restrict tests to tests with filenames matching this pattern
"
rtcl:::cli.call("rtest", doc, commandArgs(TRUE))

# vim: ft=r
